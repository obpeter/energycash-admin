//
// const {authServerUrl, resource, realm, clientSecret} = window['authConfig']
// // const {authServerUrl, resource, realm, clientSecret} = config
// // console.log("AuthConf", config)
// export const keycloakConfig = {
//     url: `${authServerUrl.replace(/\/+$/, "")}/realms/${realm}/`,
//     client_id: resource,
//     realm: realm,
//     client_secret: clientSecret,
//     // redirect_uri: "https://admin.eegfaktura.at"
//     redirect_uri: "http://localhost:3000/"
// }